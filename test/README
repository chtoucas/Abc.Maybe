README
------

The actual test project is not here but in "src" and is called Abc.Tests.

Projects here are meant to work with "eng\post-check.ps1".

Of course, one can still use the dotnet command-line directly:
> dotnet build NETSdk
> dotnet build NETSdk /p:__Max=true
> dotnet test NETSdk -f net48
Beware, this won't work with the projects NET45 and NET451 which use pre-SDK
project files.

See outdated packages (we use net48, but any other one would do the job).
> dotnet list NETSdk package --framework net48
To see what is the last edge version of Abc.Maybe.
Beware, you MUST be in the directory "test" (relative path).
> dotnet list NETSdk package --framework net48 --config .\NuGet.local.Config --outdated

## Default version.
Default restore (not necessary).
> dotnet restore NETSdk
Default build for net48.
> dotnet build NETSdk -f net48

## Specific version.
Default restore (not necessary).
> dotnet restore NETSdk /p:AbcVersion=1.0.0-edge.24241460
Build with specific version in mind.
> dotnet build NETSdk -f net48 /p:AbcVersion=1.0.0-edge.24241460


Restore packages.
> dotnet restore .\NETSdk\ --configfile .\NuGet.local.Config

nuget.exe install Abc.Maybe -Version 1.0.0-edge.2438389 -OutputDirectory ..\__\nuget-cache\

# Ne semble pas marcher en local.
nuget.exe list -Source C:\opt\code-github\Abc.Maybe\__\nuget-feed\

https://github.com/NuGet/Home/wiki/%5BSpec%5D-Fallback-package-folders
https://docs.microsoft.com/en-us/nuget/reference/nuget-config-file
https://docs.microsoft.com/en-us/dotnet/core/tools/dotnet-nuget-add-source
https://docs.microsoft.com/en-us/nuget/hosting-packages/local-feeds
https://docs.microsoft.com/en-us/nuget/reference/cli-reference/cli-ref-sources
https://medium.com/@churi.vibhav/creating-and-using-a-local-nuget-package-repository-9f19475d6af8