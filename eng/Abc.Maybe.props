<Project>

  <!-- Versioning.
    AssemblyVersion      = Major.Minor.0.0
    PackageVersion       = Major.Minor.Patch-PreReleaseLabel
    InformationalVersion = Major.Minor.Patch-PreReleaseLabel+BuildMetadata
    FileVersion          = Major.Minor.BuildNumber.RevisionNumber
    NB: BuildMetadata, BuildNumber and RevisionNumber are automatically generated.
    https://docs.microsoft.com/en-gb/dotnet/standard/library-guidance/versioning
  -->
  <PropertyGroup>
    <MajorVersion>1</MajorVersion>
    <MinorVersion>0</MinorVersion>
    <PatchVersion>0</PatchVersion>
    <!-- Format: (alpha|beta|rc)[1-9][0-9]?
      Do not use "." or "-" to separate alphas from digits. Examples:
      alpha1 < alpha2 < ... < alpha11 < ...< beta1 < ... < rc1 < ...
    -->
    <PreReleaseLabel>alpha2</PreReleaseLabel>
  </PropertyGroup>

  <!-- Package metadata & assembly info.
    See also Directory.Build.props which is loaded before this file. -->
  <PropertyGroup>
    <Title>An Option Type for C#.</Title>

    <PackageDescription>
      Provides an Option type and companion helpers (parsing, LINQ).
    </PackageDescription>
    <PackageTags>Option;Maybe;LINQ;ROP;Railway;Monad</PackageTags>
  </PropertyGroup>

</Project>